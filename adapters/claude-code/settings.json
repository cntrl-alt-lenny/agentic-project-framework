{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "description": "Adapter settings. Wires one convenience: mirroring each session's final reply to a shared location so reports need less manual relaying. It is a convenience, not a control — its absence means UNKNOWN, never that a task did not happen. See .claude/README.md.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          { "type": "command", "command": "python .claude/hooks/save_agent_reply.py" }
        ]
      }
    ]
  }
}
